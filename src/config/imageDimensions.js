/** Shared wardrobe / outfit image standards (3:4 portrait). */
export const WARDROBE_IMAGE = {
    recommendedWidth: 900,
    recommendedHeight: 1200,
    aspectRatio: 3 / 4,
    aspectRatioLabel: '3:4',
    maxFileSizeMb: 5
};

/** CSS-friendly thumb width used in tables and upload previews. */
export const WARDROBE_IMAGE_THUMB_WIDTH = '6rem';
